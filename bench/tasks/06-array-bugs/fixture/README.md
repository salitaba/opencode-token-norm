# checkout: two independent bugs

Each file exports one function with one bug. The contract:

- `f01.median(nums) — middle value; even length averages the two middle values`
- `f02.rotate(nums, k) — right-rotate nums by k places, k may exceed length`
